{
  "name": "resqai",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server,ml -c cyan,green,yellow \"npm --prefix client run dev\" \"npm --prefix server run dev\" \"python ml/app.py\"",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "seed": "node scripts/seed.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  }
}
