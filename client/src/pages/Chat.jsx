import { useEffect,useRef,useState } from 'react';import { Bot,Check,ChevronRight,Copy,History,Languages,Mic,MicOff,Plus,Send,ShieldCheck,User } from 'lucide-react';import ReactMarkdown from 'react-markdown';import toast from 'react-hot-toast';import { api } from '../api';import { useData } from '../context/DataContext';
const prompts=['Will my area face flooding this week?','What emergency supplies should I keep?','What should I do during a heatwave?','Where is the nearest shelter?'];
export default function Chat(){const {risks}=useData();const [messages,setMessages]=useState([]),[input,setInput]=useState(''),[lang,setLang]=useState('en'),[loading,setLoading]=useState(false),[sessions,setSessions]=useState([]),[sessionId,setSessionId]=useState(crypto.randomUUID()),[listening,setListening]=useState(false);const bottom=useRef();useEffect(()=>{api.get('/chat/sessions').then(r=>setSessions(r.data)).catch(()=>{})},[]);useEffect(()=>bottom.current?.scrollIntoView({behavior:'smooth'}),[messages,loading]);
 async function send(text=input){text=text.trim();if(!text||loading)return;const outgoing=[...messages,{role:'user',content:text,timestamp:new Date().toISOString()}];setMessages(outgoing);setInput('');setLoading(true);try{const {data}=await api.post('/chat',{messages:outgoing,lang,sessionId,contextData:{risks,weather:{city:'Lucknow'}}});setSessionId(data.sessionId);setMessages(m=>[...m,{role:'assistant',content:data.reply,timestamp:new Date().toISOString()}]);setSessions(s=>s.some(x=>x.sessionId===data.sessionId)?s:[{sessionId:data.sessionId,title:text.slice(0,45),updatedAt:new Date()},...s])}catch{toast.error('ResQAI is unavailable. Please try again.')}finally{setLoading(false)}}
 function voice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return toast.error('Voice input is not supported in this browser.');const r=new SR();r.lang=lang==='hi'?'hi-IN':'en-IN';r.interimResults=false;r.onstart=()=>setListening(true);r.onend=()=>setListening(false);r.onerror=()=>{setListening(false);toast.error('Could not capture speech.')};r.onresult=e=>setInput(e.results[0][0].transcript);r.start()}
 function reset(){setMessages([]);setSessionId(crypto.randomUUID())}
 return <div className="mx-auto flex h-[calc(100vh-72px)] max-w-[1500px] overflow-hidden bg-white lg:my-5 lg:h-[calc(100vh-112px)] lg:rounded-2xl lg:border lg:shadow-card"><aside className="hidden w-72 shrink-0 border-r bg-slate-50/80 p-4 md:flex md:flex-col"><button onClick={reset} className="btn-primary w-full"><Plus size={17}/> New conversation</button><div className="mt-6 flex items-center gap-2 px-2"><History size={14}/><span className="eyebrow">Recent</span></div><div className="scrollbar mt-3 flex-1 space-y-1 overflow-y-auto">{sessions.length?sessions.map(s=><button key={s.sessionId} className={`w-full rounded-xl px-3 py-3 text-left text-xs font-semibold transition ${s.sessionId===sessionId?'bg-white text-ink shadow-sm':'text-slate-500 hover:bg-white'}`} onClick={()=>{setSessionId(s.sessionId);toast('Conversation history is stored; continue with a new message.')}}>{s.title}<ChevronRight className="float-right" size={13}/></button>):<p className="px-3 text-xs leading-5 text-slate-400">Your recent conversations will appear here.</p>}</div><div className="rounded-xl bg-ink p-4 text-white"><ShieldCheck size={20} className="text-teal-300"/><p className="mt-3 text-xs font-bold">Emergency?</p><p className="mt-1 text-[11px] leading-4 text-slate-400">Call 112. AI guidance does not replace emergency services.</p></div></aside>
 <section className="flex min-w-0 flex-1 flex-col"><header className="flex h-16 shrink-0 items-center border-b px-4 sm:px-6"><span className="grid h-9 w-9 place-items-center rounded-xl bg-teal-50 text-teal-700"><Bot size={19}/></span><div className="ml-3"><b className="block text-sm text-ink">ResQAI Assistant</b><span className="flex items-center gap-1 text-[10px] font-semibold text-emerald-600"><i className="h-1.5 w-1.5 rounded-full bg-emerald-500"/> Ready to help</span></div><button onClick={()=>setLang(l=>l==='en'?'hi':'en')} className="btn-ghost ml-auto !py-2"><Languages size={15}/>{lang==='en'?'English':'हिन्दी'}</button></header>
 <div className="scrollbar flex-1 overflow-y-auto px-4 py-6 sm:px-8">{!messages.length?<div className="mx-auto flex h-full max-w-2xl flex-col items-center justify-center text-center"><span className="grid h-16 w-16 place-items-center rounded-2xl bg-ink text-white shadow-xl"><Bot size={30}/></span><p className="eyebrow mt-6">Disaster preparedness assistant</p><h1 className="mt-2 text-3xl font-extrabold tracking-tight text-ink">What can I help you prepare for?</h1><p className="mt-3 max-w-lg text-sm leading-6 text-slate-500">Get clear, local and actionable guidance based on current conditions. In an emergency, always contact official services first.</p><div className="mt-8 grid w-full gap-3 sm:grid-cols-2">{prompts.map(p=><button onClick={()=>send(p)} key={p} className="group rounded-xl border bg-white p-4 text-left text-xs font-semibold text-slate-600 transition hover:border-teal-400 hover:shadow-md">{p}<ChevronRight className="float-right text-slate-300 group-hover:text-teal-600" size={15}/></button>)}</div></div>:<div className="mx-auto max-w-3xl space-y-6">{messages.map((m,i)=><div key={i} className={`flex gap-3 ${m.role==='user'?'justify-end':''}`}>{m.role==='assistant'&&<span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-ink text-white"><Bot size={16}/></span>}<div className={`group max-w-[82%] rounded-2xl px-4 py-3 text-sm leading-6 ${m.role==='user'?'rounded-br-sm bg-teal-700 text-white':'rounded-tl-sm bg-slate-100 text-slate-700'}`}><div className="prose-chat"><ReactMarkdown>{m.content}</ReactMarkdown></div>{m.role==='assistant'&&<button onClick={()=>{navigator.clipboard.writeText(m.content);toast.success('Copied')}} className="mt-2 flex items-center gap-1 text-[10px] font-bold text-slate-400 opacity-0 transition group-hover:opacity-100"><Copy size={11}/> Copy</button>}</div>{m.role==='user'&&<span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-slate-200 text-slate-600"><User size={16}/></span>}</div>)}{loading&&<div className="flex gap-3"><span className="grid h-8 w-8 place-items-center rounded-lg bg-ink text-white"><Bot size={16}/></span><div className="flex gap-1 rounded-2xl rounded-tl-sm bg-slate-100 px-4 py-4">{[0,1,2].map(i=><i key={i} className="pulse-dot h-2 w-2 rounded-full bg-slate-500"/>)}</div></div>}<div ref={bottom}/></div>}</div>
 <div className="shrink-0 border-t bg-white p-3 sm:p-5"><div className="mx-auto flex max-w-3xl items-end gap-2 rounded-2xl border bg-white p-2 shadow-[0_8px_30px_rgba(15,23,42,.08)] focus-within:border-teal-500"><textarea rows={1} value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder={lang==='hi'?'अपना सवाल लिखें…':'Ask about risks, safety or preparedness…'} className="max-h-32 min-h-[44px] flex-1 resize-none border-0 px-3 py-3 text-sm outline-none"/><button onClick={voice} className={`grid h-10 w-10 place-items-center rounded-xl ${listening?'bg-red-50 text-red-500':'text-slate-400 hover:bg-slate-100'}`}>{listening?<MicOff size={18}/>:<Mic size={18}/>}</button><button disabled={!input.trim()||loading} onClick={()=>send()} className="grid h-10 w-10 place-items-center rounded-xl bg-ink text-white disabled:opacity-30"><Send size={17}/></button></div><p className="mt-2 text-center text-[10px] text-slate-400">ResQAI can make mistakes. Verify critical advice with local authorities.</p></div></section></div>}

