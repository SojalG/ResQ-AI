import React,{lazy,Suspense} from 'react';import ReactDOM from 'react-dom/client';import {BrowserRouter,Navigate,Route,Routes,useLocation} from 'react-router-dom';import {AnimatePresence,motion} from 'framer-motion';import {Toaster} from 'react-hot-toast';import './index.css';import Navbar from './components/Navbar';import {DataProvider} from './context/DataContext';
const Dashboard=lazy(()=>import('./pages/Dashboard'));const Chat=lazy(()=>import('./pages/Chat'));const Report=lazy(()=>import('./pages/Report'));const Admin=lazy(()=>import('./pages/Admin'));
class ErrorBoundary extends React.Component{state={error:null};static getDerivedStateFromError(error){return{error}}render(){return this.state.error?<div className="mx-auto mt-20 max-w-lg card p-8 text-center"><h2 className="text-xl font-bold">This panel hit a snag</h2><p className="mt-2 text-sm text-slate-500">{this.state.error.message}</p><button className="btn-primary mt-5" onClick={()=>location.reload()}>Reload page</button></div>:this.props.children}}
function AppRoutes(){const l=useLocation();return <><Navbar/><AnimatePresence mode="wait"><motion.main className="page-enter" key={l.pathname} initial={{opacity:0,y:6}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-4}} transition={{duration:.22}}><ErrorBoundary><Suspense fallback={<div className="mx-auto max-w-[1500px] p-6"><div className="skeleton h-32"/><div className="mt-5 grid gap-4 md:grid-cols-3"><div className="skeleton h-64"/><div className="skeleton h-64"/><div className="skeleton h-64"/></div></div>}><Routes location={l}><Route path="/dashboard" element={<Dashboard/>}/><Route path="/chat" element={<Chat/>}/><Route path="/report" element={<Report/>}/><Route path="/admin" element={<Admin/>}/><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes></Suspense></ErrorBoundary></motion.main></AnimatePresence></>}
ReactDOM.createRoot(document.getElementById('root')).render(<React.StrictMode><BrowserRouter><DataProvider><div className="app-bg"><AppRoutes/><Toaster position="top-right" toastOptions={{style:{borderRadius:'12px',fontSize:'13px',fontWeight:600}}}/></div></DataProvider></BrowserRouter></React.StrictMode>);
